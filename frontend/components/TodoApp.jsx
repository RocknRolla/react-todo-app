let React = require('react');

let TodoApp = React.createClass({
    render: function () {
        return (
            <div>
                TodoApp Component
            </div>
        );
    }
});

module.exports = {TodoApp};